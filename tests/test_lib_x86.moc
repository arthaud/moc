#include "lib_x86.moc"

int main()
{
    int* p = (int*) mmap(NULL, 200000, 3, 34, -1, 0);
    *p = 1;
    p = p + 1;
    *p = 2;
    p = p - 1;

    munmap((void*)p, 200000);

    int x = 5;
    x = x + 1;

    return x;
}
