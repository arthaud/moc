#include "lib/craps-header-kernelmode.moc"
#include "lib/craps.moc"

int main() {
    int x = 1;
    int y = 2;
    10 + x < 100 + x; // code < code
    10 + x < x; // code < value
    x < 100 - x; // value < code
    x < y; // value < value
}
